\$ First Example
fun hello (): nothing
    var x : int;
    fun goodbye(): nothing
        var y : int;
    {
        if y > 1 then
            puts("Nah!");
        puts("Goodbye world!\n");
    }
{
    if x < 1 then 
        puts("Yes it is");
    puts("Hello world!\n");
    goodbye();
}